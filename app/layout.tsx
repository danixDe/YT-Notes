import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Notes Generator - Transform YouTube Videos into Organized Notes',
  description: 'Generate comprehensive, organized notes from any YouTube video instantly. Perfect for students, professionals, and lifelong learners.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}